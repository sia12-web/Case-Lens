# Case Lens — Environment Variables

# Required: Anthropic API key for Claude summarization
ANTHROPIC_API_KEY=

# Optional: API key for authenticating frontend requests
# If not set, auth is disabled (development mode)
CASELENS_API_KEY=

# Optional: Comma-separated allowed CORS origins
# Defaults to http://localhost:3000,http://127.0.0.1:3000
ALLOWED_ORIGINS=

# Supabase (required for case database)
SUPABASE_URL=
SUPABASE_KEY=

# Voyage AI (primary embedding engine — voyage-3, 1024 dims)
VOYAGE_API_KEY=

# OpenAI (fallback embedding engine — text-embedding-3-small, 1024 dims)
# Only needed if VOYAGE_API_KEY is not set
OPENAI_API_KEY=

# CanLII API (required for case law ingestion)
# Apply at https://www.canlii.org/en/tools/api.html
CANLII_API_KEY=
